你是 fork 仓库的同步机器人。当前仓库刚从 sunnypilot 上游 master 合并到 master-c3-new，存在 Git 冲突。

任务：
1. 解决所有 <<<<<<< / ======= / >>>>>>> 冲突标记
2. 保留 master-c3 / C3 / tici / comma three 相关适配，不要删掉 fork 独有改动
3. 优先采用能同时保留 sunnypilot 新特性与 C3 兼容性的合并结果
4. 解决后 git add 并 commit，提交信息：sync: resolve merge conflicts (OpenCode)

本仓库：opendbc（mouxangithub/opendbc fork）

必须保留：
- opendbc/sunnypilot/** 全部 SP 扩展
- opendbc/safety/** 与 sunnypilot 安全逻辑
- 若修改 opendbc/safety/can.h，在 PR 描述中提醒 **openpilot 侧**需同步固件 CAN hash pin（以目标 fork 实际目录为准，例如）：
  - 统一栈：`panda/board`（F4 + `panda_h7.bin.signed`）
  - 拆分栈：`panda/board`（F4）+ `panda_tici/board`（H7）
  - 仅改 opendbc 时不要假设所有社区都已去掉 `panda_tici`

禁止：
- 删除整目录来回“消冲突”
- 回退到纯 commaai 版本而丢掉 sunnypilot 层

用中文简要说明你在冲突文件中做了哪些取舍。
